[/Script/UnrealEd.LevelEditorPlaySettings]
; Play-In-Editor must spawn the player at the PlayerStart — never at the floating
; editor camera (which drops the hero into mid-air/void and breaks every feel test).
LastExecutedPlayModeLocation=PlayLocation_DefaultPlayerStart
